{
  "id": "cre",
  "name": "HookBus Publisher",
  "description": "Publishes OpenClaw before_tool_call, after_tool_call, llm_input, and llm_output events to HookBus. Enforces consolidated verdict on tool calls and LLM calls; emits observation events for post-tool + post-LLM.",
  "version": "0.5.1",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
